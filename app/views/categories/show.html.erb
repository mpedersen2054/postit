<% cat_header = link_to('All Posts', posts_path) + "      &raquo; #{@category.name}".html_safe %>


<%= render 'shared/content_title', title: cat_header %>

<% @category.posts.sort_by{|x| x.total_votes}.reverse.each do |post| %>
  <%= render 'posts/post', post: post %>
<% end %>
